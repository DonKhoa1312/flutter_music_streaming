import 'package:flutter/material.dart';

class Minicardsong extends StatelessWidget {
  const Minicardsong({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red,
     
    );
  }
}
