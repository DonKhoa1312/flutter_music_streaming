class ModelMusic {
  const ModelMusic({
    required this.title,
    required this.id,
    required this.audiourl,
    required this.channel,
    required this.thumb,
  });
  final String title, id, channel, thumb,audiourl;
}
